/**
 * Alias estable para la maquina de estados.
 *
 * Mantiene compatibilidad con la carpeta `state/` creada en la fase anterior y
 * permite que futuras maquinas especializadas vivan aqui sin romper imports.
 */
export { ZeninStateMachine } from "../state/state-machine.mjs";
