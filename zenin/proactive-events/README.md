# proactive-events

Responsabilidad: crear eventos inteligentes no aleatorios.

Cada evento tiene animacion, mensaje base, expresion y despedida. No se muestra directamente; pasa por `presence-engine` y `scheduler`.

Relaciones:

- consume historial, observaciones, logros y relacion;
- alimenta `behavior-engine` y `notification-engine`.
